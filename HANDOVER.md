# 交接文件

> 撰寫日期：2026-09-09　　對象：接手本專案的開發者／AI agent（Codex 等）
> **請先完整讀完本文件再動任何一行程式。**本專案的多數設計是「刻意」的，
> 有些看起來可以優化的地方，動了會直接破壞學習效果。

---

## 0. TL;DR — 接手後最容易犯的五個錯

| # | 千萬不要 | 為什麼 |
|---|---|---|
| 1 | 在 `docs/index.html` 加入任何外部資源（CDN、字型、圖片） | 使用者在**捷運隧道**讀這頁，必須離線可用。目前外部請求數 = **0**，請維持 |
| 2 | commit 前不跑語法檢查 | 曾發生字串literal 被真實換行切斷 → **整份 inline script 死掉**，但 HTML 外觀完全正常。clone 後**先跑 `sh scripts/install-hooks.sh`** |
| 3 | 幫「通勤泛讀」加上查字典／翻譯／計時功能 | 泛讀的設計目的就是**不查字典、不計時**。加了會直接破壞流暢度訓練 |
| 4 | 把生字筆記寫成「單字＝中文意思」 | 明確被使用者否決過。見 §4.3 |
| 5 | 出測驗題時讓句子自己解釋目標字 | 已犯過一次，導致整輪測驗無效。見 §3.4 |

---

## 1. 專案是什麼

一位台灣工程師（金流／帳務系統開發）的**英文閱讀能力加強計畫**，
為期 4 週，平日限定，全部在公司電腦與捷運通勤時間進行。

- **正式期間**：2026-09-10（四）~ 2026-10-08（四）
- **Day 0 加碼日**：2026-09-09（已完成診斷）
- **線上版**：https://keieykd.github.io/english-reading/
- **repo**：https://github.com/keieykd/english-reading （public）

### 目標權重

| 目標 | 權重 | 備註 |
|---|---|---|
| 泛用英文閱讀 | 3/5 | 主幹 |
| 旅遊實用英文 | 2/5 | 對準 **2026-11-20 日本行** |
| 多益 | 0（本期） | 尚未報名，**這 4 週不排考題** |
| 口說 | 僅輸入端 | 公司電腦與捷運都不能出聲，物理上做不到 |

### 使用者的既定行程（影響教材主題）

| 期間 | 目的地 |
|---|---|
| 2026-09-12 ~ 09-15 | 🇹🇭 泰國（4 天，週六去週二回） |
| 2026-11-20 ~ 11-29 | 🇯🇵 日本 ← **旅遊教材的目標日** |
| 2027-01-09 ~ 01-18 | 🇯🇵 日本 |
| 2027-05-24 ~ 05-28 | 🇮🇩 峇里島 |

---

## 2. 每日運作方式

```
通勤 30 分鐘   泛讀（量）  捷運，約一半站著
                          不查字典、不求全懂、讀完就好
                          內容切成 3–5 分鐘 chunk
                          ★ 不測速、不計時
公司 30 分鐘   精讀（質）  拆句子、作答
                          作答貼回 Claude 對話批改
                          批改時必須追問「你怎麼判斷的」
```

- **教材一週生成一次（週一）** — 因為去程通勤發生在到公司之前，
  當天才生成的話早上那 30 分鐘會空著
- **缺席不補課**，隔天照原排程繼續（補課債是習慣計畫的頭號死因）
- **站著讀不完不算缺席**

### 出席計算（已扣除假期）

```
窗口 9/10 ~ 10/9 平日總數     22 天
  − 泰國排休（9/14、9/15）    -2
  − 國定假日（9/25 中秋、
    9/28 教師節、10/9 補假）  -3
─────────────────────────────────
  實際上課日                   17 天
  門檻（80%）                  14 天   ← 可缺 3 天
```

國定假日依 **人事行政總處「115年中華民國政府行政機關辦公日曆表」**
（data.gov.tw dataset 14718，欄位「是否放假」0=上班 / 2=放假），非憑印象。
**若要延長計畫或改窗口，必須重新查這份資料。**

出席以 **git commit 時間戳**認定，不採自我回報。

---

## 3. 學習者剖面（最重要的一節）

這節是 Day 0 診斷的結論。**接手者的所有教材決策都應以此為依據。**
原始逐題分析見 `errors.md`。

### 3.1 硬數據

| 指標 | 數值 |
|---|---|
| 速讀基準 | **74 wpm**（194 字 / 158 秒），理解 **2/2 全對** |
| 診斷卷 | **5/9** |
| 　細節・條件追蹤 | **5/5 全對** |
| 　主旨・推論・長句 | **0/4 全錯** |
| 三篇短文用時 | 10 分鐘 |
| 錯誤分類 | V-f 1｜V-c 0｜S 0｜I 2｜L 1｜C 0 |

### 3.2 核心結論：他的理解仰賴「語境冗餘」

這是整份診斷最重要的發現，用兩輪測驗確認過：

| 測驗 | 句子有沒有多餘線索 | 結果 |
|---|---|---|
| 第一輪五題 | **有**（我出題時不慎洩答案） | **5/5** — 但他自述三題「猜的」 |
| 第二輪五題 | **無**（每句只剩目標字一個線索） | **2/5** — 且兩題答對皆自述「猜的」 |

**當句子有多餘線索時，他能用語境推出答案——這是真本事，不是運氣。**
但他自己會感受成「猜的」，因為他知道自己繞過了那個字。
**當關鍵字是唯一資訊來源時，理解就崩掉。**

而**條款文體正是低冗餘文體**（每個字都承載法律效力，沒有廢話可推），
這解釋了為什麼他覺得條款特別難，也解釋了他自述的「50% 單字不懂」。

### 3.3 他有兩種不同的失敗，處方相反

| 型 | 例子 | 特徵 | 處方 |
|---|---|---|---|
| **(i) 不認識** | `thereafter` | 他自己列得出來 | 背。封閉集合，背得完 |
| **(ii) 認識但不會運算** | `unless`、`where` | 說得出意思，代不進案例 | **背單字無效**，要教固定動作 |

型 (ii) 的證據：
- R2 他寫「除非有另外預定導覽?」→ **結構讀對了，卻選了相反結論**
- R3 的 `where` 第一輪答對且標「有把握」，第二輪就用不出來

**型 (ii) 的處方：三步驟代入法**
1. 把句子改寫成「條件 → 結果」
2. **把案例代進條件，只回答「是」或「不是」** ← 他三題全停在這步之前
3. 照第 2 步的答案取結果

### 3.4 ⚠️ 兩個關於「量測」的陷阱（都已踩過）

**陷阱一：他的自我盤點會系統性漏掉功能字。**

他事後表示 `unless` 也不知道意思——但那個字**不在他列的 11 個生字裡**。

| | 例子 | 為什麼 |
|---|---|---|
| 他列得出來 | itinerary、administrative、interruption | 外形陌生，**會自己舉手** |
| 他列不出來 | unless、less、fail to | 外形眼熟，**不觸發「我不懂」的警報** |

→ **不要再用「列出不認識的字」當診斷工具**，它只抓得到內容字。
改用**功能探測**：不問「你認不認識」，問「這句話是什麼意思／誰要付錢／可不可以」。

**陷阱二：測驗題不得讓句子自己解釋目標字。**

第一輪五題他全對，但其中三題的句子把答案講了出來，最嚴重的是：

> `Early check-in is subject to availability **and cannot be guaranteed**.`

後半句直接把 `subject to` 翻譯掉了 = 送分題，**測不出任何東西**。

→ **往後所有探測題，句子裡不得出現改述、同位語或解釋目標字的子句。**
→ **每題必須附「有把握／用猜的」**，答對且有把握 ≠ 答對但用猜的。

**陷阱三：常識也會洩答案（第三輪才發現，規則要更嚴）。**

第三輪 T1–T3 我已經拿掉了句內改述，他仍 3/3 全對——但他自述
「不知道這些單字意思，按常理回答」。稽核後確認**三題都能靠世界知識答對**：

| 題 | 目標字 | 為什麼不需要那個字 |
|---|---|---|
| T1 | provided | 「free」+ 25>20 +「行李超重要加錢」的生活常識 |
| T2 | unless | 「已經特價就不會再打折」是純世界知識 |
| T3 | thereafter | **第一句自己就講完了**，thereafter 那句多餘 |

決定性對照 —— **同一個 `unless`，兩次結果相反**：

| 測驗 | 常識指向 | 結果 |
|---|---|---|
| R2 `Entry is free unless you book a guided tour` | 中性／誤導 | ❌ |
| T2 `The discount applies unless already on sale` | 正解 | ✅ |

**差別在常識，不在他學會了 unless。**

→ **探測題的完整設計規則（三條全滿足才算有效）**：
> 1. 句子裡沒有改述或解釋目標字的子句
> 2. **世界知識不能預測答案** —— 最理想是讓常識指向**錯誤**方向，
>    這樣只有讀懂那個字才答得對
> 3. 每題附「有把握／用猜的」

→ **目前狀態：`provided` / `unless` / `thereafter` 仍未確認習得。**
接手者若要驗證，必須重新出符合上述三條的題目。

### 3.5 處方優先序（已定案）

| 優先 | 處方 | 依據 |
|---|---|---|
| **P1** | **邏輯骨架字**（約 25 個封閉集合）＋ **三步驟代入法** | 低冗餘測驗 2/5，且兩題自述猜的 |
| **P2** | **回原文找依據**：每題作答必須附依據句 | B2「只知道是停車規則，但不知道罰款」＝ 用常識填補空缺 |
| **P3** | **主旨三步法**（重複名詞／位置／刪去只出現一段的選項） | A1「老實說沒看出來，亂猜的」＝ 無方法 |
| **P4** | **速度：不主動推**，讓它自然上升 | 見下 |

> **⚠️ 不要把速度提前。** 他在慢速（74 wpm）時理解 2/2 全對，
> 在較快時掉到 5/9。**理解力對速度極度敏感。**
> 在策略缺口補起來之前推速度，只會讓他更快地亂猜。

**刻意不做**：長句拆解訓練（S 類錯誤 = 0，不預先建置）、聽力、寫作。

---

## 4. 內容撰寫規範

### 4.1 語言與語氣
- 全部繁體中文（台灣用語），英文教材內容為英文
- 使用者是工程師，不要童趣化、不要過度鼓勵
- 誠實優先：測驗出錯要認、判斷錯要更正

### 4.2 教材難度
- 泛讀 chunk：3–5 分鐘一塊，單手手機可讀，段落 2–3 行
- 精讀短文：150–200 字
- 條款類材料是**主力題材**（訂房信、機票條款、退換貨政策），
  因為那是他最弱、也最實用的文體（旅遊 + 他的金流工作）

### 4.3 生字筆記格式（使用者主動提供並認同的規則）

**不要只貼一個中文意思。** 要寫「它跟誰搭、放在什麼位置、下次怎麼判讀」。

- **寫完整卡**：位置會變意思（concerned）、介系詞決定意思（be concerned about/with）、
  條款特殊義（provided、subject to）
- **寫中文就好**：純內容名詞（baht、itinerary、sectors、adapter）
- **每張卡必須有「下次怎麼判讀」那一行**，沒有不算寫完

分辨方法：「這個字換個句子還會有別的意思嗎？」不會 → 寫中文。會 → 寫完整卡。

完整規則與 `concerned` 範本見 `vocab.md`。

### 4.4 錯誤分類（批改時逐題歸類，記入 `progress.md`）

| 代碼 | 類型 | 定義 |
|---|---|---|
| V-c | 字彙・內容 | 內容字不認識。開放集合，遇到就好 |
| V-f | 字彙・功能 | 邏輯字不認識。**封閉集合，要精熟** |
| S | 句構 | 字都懂，句子拆不開 |
| I | 推論 | 字句都懂，沒把分散資訊接起來 |
| L | 定位 | 答案來源找錯地方 |
| C | 粗心 | 讀對了但選錯 |

用途：V-f 多 → 攻邏輯骨架（最高投報）；I 多 → 加推論題；C 多 → 放慢作答，非能力問題。

---

## 5. 技術架構

### 5.1 檔案

```
english-reading/
├── HANDOVER.md          ← 本文件
├── PLAN.md              4 週計畫（目標、行事曆、出席、教學法、處方）
├── progress.md          出席表、WPM 追蹤、錯誤分類累計、每週檢討
├── errors.md            逐題錯誤診斷（Day 0 完整記錄）
├── vocab.md             生字本規則 + concerned 範本 + 泰國蒐集任務
├── diagnostic-01.md     診斷卷原文（網頁版才是主要作答介面）
├── days/
│   ├── 2026-09-10.md    Day 1 泰國實戰：抵達與移動
│   └── 2026-09-11.md    Day 2 泰國實戰：吃、買、出狀況
├── docs/
│   └── index.html       ★ 主要產品。單一檔案、零外部請求
├── index.html           根目錄轉址頁 → ./docs/
├── .nojekyll
└── scripts/
    ├── pre-commit       JS 語法檢查
    └── install-hooks.sh clone 後必跑
```

### 5.2 `docs/index.html` 結構

**單一 HTML 檔，約 1500 行，內容全內嵌，外部請求 = 0。**

七個分頁（`nav button[data-p]` ↔ `section.panel#id`）：

| data-p | 內容 |
|---|---|
| `now` | **今天做什麼** — 依日期自動判斷狀態並給編號步驟 |
| `diag` | 診斷卷（速讀基準 + 三篇短文 + D1–D3） |
| `glue` | **邏輯字** — 三步驟代入法、六個會騙人的字、25 個骨架字、三輪探測 |
| `cheat` | 隨身卡（泰國七類情境句型） |
| `d1` `d2` | Day 1 / Day 2 教材 |
| `plan` | 規則、目標、生字寫法、17 天排程 |

**核心資料結構** — `SCHED`（JS 物件，鍵為 `YYYY-MM-DD`）：

```js
{ t:"study", n:1, theme:"…", tab:"d1" }   // 正式上課日，n 計入分母
{ t:"day0",  theme:"…", tab:"diag" }      // 加碼日，不計入分母
{ t:"trip" }                              // 排休
{ t:"holiday", why:"中秋節" }              // 國定假日
```

`render()` 依今天日期查 `SCHED` 決定卡片；查無資料時再依週末／窗口前後給預設。
**新增上課日只要在 `SCHED` 加一列並在對應分頁補內容即可。**

### 5.3 localStorage 鍵（全部 `er.` 前綴）

| 鍵 | 內容 |
|---|---|
| `er.done` | 已完成日期，逗號分隔 |
| `er.seen` | 是否看過「怎麼用」說明 |
| `er.wpm` / `er.wpmSec` | 速讀基準結果 |
| `er.diagT0` | 診斷卷計時起點 |
| `er.d.*` | 診斷卷作答（A1–C3、S1–S2、D1–D3） |
| `er.p.*` | 邏輯字第一輪 P1–P5 |
| `er.r.*` | 邏輯字第二輪 R1–R5 + 把握度 R1c–R5c |

**所有存取都包在 `try/catch` 並有記憶體 fallback**（無痕視窗會擋 localStorage）。
新增功能請沿用 `store(k, v)` 這個 helper，不要直接呼叫 `localStorage`。

### 5.4 部署

- **GitHub Pages 來源 = repo 根目錄**（不是 `/docs`）
- 根目錄 `index.html` 是 meta-refresh 轉址頁 → `./docs/`
- 兩種 Pages 設定（root 或 /docs）**都能運作**，這是刻意的雙保險
- push 後約 **30–60 秒**生效，用以下指令確認：

```bash
curl -s https://keieykd.github.io/english-reading/docs/ | grep -c "你要找的字串"
```

---

## 6. 🔥 已知地雷（全部踩過，請勿重蹈）

### 6.1 檔案是 CRLF
`docs/index.html` 使用 CRLF 行尾。用 Python 做字串取代時，
`split('\n')` 會留下 `\r`，`b'...\n...'` 的位元組比對會失敗。
**改動大段內容時優先用編輯工具做精確字串取代，不要用腳本硬幹。**

### 6.2 跳脫字元被寫成真實換行 → 全站腳本死亡
曾把 `lines.join("\n")` 寫成字面換行，切斷字串literal，
**整份 inline script 語法錯誤 → 今天卡片、分頁切換、診斷卷全部失效**，
但 HTML 外觀完全正常，肉眼極難察覺。

→ **clone 後第一件事：**
```bash
sh scripts/install-hooks.sh
```
該 hook 會抽出 inline script 跑 `node --check`，語法錯誤直接擋下 commit。
已用反向測試確認會擋。

### 6.3 GitHub 推送必須用 noreply 信箱
public repo 直接用 `keieykd@gmail.com` 會被擋：
`GH007: Your push would publish a private email address`

repo 的 local 設定（**不要改成 global**，這是公司電腦）：
```
user.name  = Eason Chen
user.email = 7703663+keieykd@users.noreply.github.com
```
全域 git 身分**必須保持未設定**，否則未來公司專案的 commit 會掛上私人信箱。

### 6.4 速讀計時的誤觸防呆
> 400 wpm 判定為不合理（陌生文本上不可能），不記錄並要求重測。
新增任何計時功能請比照處理。

### 6.5 診斷卷的 15 分鐘計時**不能**用來換算閱讀速度
那段計時包含作答時間。速度必須用獨立的**純閱讀計時**協定：
按開始才顯示文章 → 讀完停錶 → **文章自動收起防止回頭** → 才作答理解題。

---

## 7. 目前進度與待辦

### 已完成
- [x] 4 週計畫定案（目標、時間、行事曆、出席、驗收）
- [x] Day 0 診斷（速讀 74 wpm、診斷卷 5/9、兩輪邏輯字探測）
- [x] 學習者剖面與處方定案
- [x] Day 1（9/10）、Day 2（9/11）泰國實戰教材
- [x] 網頁：今天卡片、診斷卷、邏輯字、隨身卡、計畫分頁
- [x] 莫蘭迪配色、微軟正黑體、RWD、離線可讀

### 待辦（依時間順序）

1. **T1–T3 三步驟代入法練習** — 已出題，**使用者尚未作答**。
   收到後檢查他有沒有執行「第 2 步」，而不只是看答案對錯。
2. **9/12–9/15 泰國蒐集任務** — 他會拍 3–5 張看不懂的英文照片回來，
   **直接轉成第 2 週教材**（那是真實環境中真正卡住的英文）。
3. **9/16（三）泛用閱讀正式開課** — 需依 §3.5 處方生成 W2 教材：
   邏輯骨架字為主線、每題要求附依據句、主旨三步法。
4. **每週最後一個上課日**：速讀週測（新文章、同協定）+ 5 題回收檢核。
5. **第 2 週起**：每天材料回收 3–5 個先前遇過的字，**舊字新句**。
6. **10/8（四）驗收**：`diagnostic-02` 重測 + 出席率（依 commit log）。
   **重測的 D2 題不可再用「列出不認識的字」**（見 §3.4 陷阱一）。

### 尚未定案（需使用者決定，不要自行決定）
- 是否把窗口從 10/8 延長到 10/16（17 天 → 22 天，補回假日吃掉的量）
- 是否在家加 10 分鐘朗讀（否則「跟老外聊天」這個目標本期不會有進展）

---

## 8. 使用者互動守則

- 他**在上班**，用公司電腦，需要畫面低調（純文字、無影音）
- 他會**跳過冗長的說明**。指令要短、要具體、要有明確格式
- **介面一致性很重要**：他做診斷卷很順（有選項可點、有產生答案鍵），
  但看到純文字的題目就回「我不懂要回答什麼」。
  **新增任何需要他作答的東西，一律沿用「可點選 + 產生答案一鍵複製」的模式。**
- 他會**自發標註「猜的」** —— 這是極有價值的後設認知行為，
  已制度化為介面上的「有把握／用猜的」。**請保留並鼓勵。**
- 批改時**必須追問「你怎麼判斷的」** —— 那三句回答的診斷價值遠高於選擇題答案本身
