# 給 Codex 的起手訊息

把下面整段複製貼給 Codex 當第一則訊息。

---

```
這個 repo 是一位台灣工程師的英文閱讀加強計畫（4 週，平日限定，
在公司電腦與捷運通勤時間進行）。線上版：
https://keieykd.github.io/english-reading/

我要你接手繼續開發。開始前有三件事必須先做：

【1】完整讀完 HANDOVER.md，不要略讀。
    特別是這三節，讀完再動任何一行程式：
    - §3 學習者剖面 —— 所有教材決策的依據
    - §6 已知地雷 —— 六個踩過的坑
    - §9 前一位開發者犯過的錯誤 —— 20 條，每條都真的發生過

    這個專案的多數設計是刻意的。有些看起來可以優化的地方，
    動了會直接破壞學習效果（例如：通勤泛讀不能加查字典或計時）。

【2】執行 `sh scripts/install-hooks.sh`
    .git/hooks/ 不會被 clone。那個 hook 擋的是一個會讓
    整份 inline script 靜默死亡、但 HTML 外觀完全正常的 bug。
    不裝就等於沒有防護。

【3】確認你理解這條硬約束：
    docs/index.html 的外部請求數必須維持 0（使用者在捷運隧道讀這頁）。
    改完用這個指令自我驗證，結果必須是 0：
    curl -s https://keieykd.github.io/english-reading/docs/ | grep -cE 'https?://|src=|@import|url\('

做完這三件事，請先回覆我三件事，不要直接開始寫程式：
  (a) 用你自己的話講一次「語境冗餘」這個核心發現是什麼
  (b) 說出探測題設計的三條規則
  (c) 說出目前第一優先的處方是什麼、為什麼速度被排到第四

確認你真的讀懂了，我們再往下。

—— 接著要做的第一件事 ——

HANDOVER.md §7 有待辦清單。目前最優先的是：

  「W2 教材（9/16 起）」

依 §3.5 的處方生成，主線是邏輯骨架字與三步驟代入法，
每題必須要求附上原文依據句。

—— 兩件你不可以自己決定的事 ——

§7 末尾列了兩項，必須問我，不要自行決定：
  1. 窗口要不要從 10/8 延長到 10/16
  2. 要不要在家加 10 分鐘朗讀

—— 一個尚未解決的問題 ——

provided / unless / thereafter 這三個字「仍未確認習得」。
我做過三輪測驗都無效（前兩輪句內洩題、第三輪常識洩題）。
如果你要重測，題目必須同時滿足 §3.4 的三條規則，
其中最難的是第二條：世界知識不能預測答案，
最理想是讓常識指向錯誤方向。
```

---

## 補充：你可以怎麼驗收 Codex 的產出

| 檢查點 | 指令／方法 |
|---|---|
| 外部請求仍為 0 | `curl -s <網址> \| grep -cE 'https?://\|src=\|@import\|url\('` |
| JS 語法正確 | commit 時 pre-commit hook 會自動擋；本機可跑 `node --check` |
| 分頁數正確 | `curl -s <網址> \| grep -c 'role="tab"'` |
| 上線生效 | Pages 有 30–60 秒延遲，**要真的 curl 確認**，不要相信「已推送」 |

## 補充：如果 Codex 提議改動這些，先擋下來問清楚

- 幫通勤泛讀加查字典、翻譯、或計時 → **違反核心設計**
- 把生字筆記改成「單字＝中文意思」 → **使用者明確否決過**
- 把閱讀速度訓練提前 → **數據顯示他理解力對速度極度敏感**
- 加聽力或寫作 → **目標是閱讀，每天只有 60 分鐘**
- 引入任何前端框架或 CDN → **離線需求**
